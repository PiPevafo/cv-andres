---
import { Image } from 'astro:assets';

const { src, width=100, height=100, alt = "image" } = Astro.props;
---

<Image src={src} width={width} height={height} alt={alt} class:list={["image"]} loading={"lazy"}/>

<style>
    .image {
        width: 100%;
        height: auto;
        margin: 0.3rem 0rem;
    }
</style>

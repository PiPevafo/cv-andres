---
import { Image } from 'astro:assets';

const { src, alt = "image" } = Astro.props;
---

<Image src={src} alt={alt} class:list={["image"]} loading={"lazy"}/>

<style>
    .image {
        width:max-content;
        height: auto;
        margin: 0.3rem 0rem;
    }
</style>
